#include<iostream>
using namespace std;

typedef struct queue
{
      
};

int main()
{
    #if ONLINE_JUDGE
    #else
    freopen("input.txt", "r", stdin);
    #endif
    int n, m, k, q;
    cin >> n >> m >> k >> q;
    return 0;
}
