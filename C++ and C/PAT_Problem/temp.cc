#include<iostream>
using namespace std;
#include<vector>

int main()
{
    #if ONLINE_JUDGE
    #else
    freopen("input.txt", "r", stdin);
    #endif
    int n, m, k, s, e;
    cin >> n >> m >> k;
    vector<int> *cs = new vector<int> v;[n];
    for (int i = 0; i < n; ++i)
        cs[i].next = NULL;
    for (int i = 0; i < m; ++i)
    {
        cin >> s >> e;
        cs[s].next = 
    }
    return 0;
}
